============
Installation
============

At the command line::

    $ pip install asgard-agent

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv asgard-agent
    $ pip install asgard-agent
